import * as React from 'react';

import LinkAsButton from './LinkAsButton';
import styled from 'styled-components';

const LinkAsButtonDisabled = styled(LinkAsButton)<{ disabled?: boolean }>``;

export default LinkAsButtonDisabled;
